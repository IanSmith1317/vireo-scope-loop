[tool:pytest]
testpaths = tests
asyncio_mode = auto

[coverage:run]
source = .
omit =
    tests/*
    .venv/*
    setup.cfg

[coverage:report]
show_missing = true
exclude_lines =
    if __name__ == .__main__.
    pragma: no cover
